client:
  type: mqtt
  config:
    topic: "test"
    broker: "tcp://localhost:1886"
messages:
  test:
    request:
      schema: "path/to/schema.json"
      constants:
        - name: dotted.path
          value: 1.2
        - name: other.field
          value: "Hello World"
      variables:
        - name: dotted.path
          generator:
            numeric-generator:
              base: 100
              amp: 10
    response: {}
execution:
  # idk how i want to do that -> just have the steps for the load generation